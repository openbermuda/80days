
class Z:

    def dtest(infile):

        for x in self.generate(infile):
            for a in x:
                yield a


    def btest(infile):

        for x in infile:

            if a == '#':
                yield a
            else:

            

    
